
<%= link_to "Edit Profile", edit_user_path(@user) %>
<div id="user-info">
  <li>User Name: <%= @user.name %></li>
  <li>User Email: <%= @user.email %></li>
</div>

<% if @messages %>
  <div id="messages">
    <ol>
      <%= render partial: 'users/message', collection: @messages %>
    </ol>
  </div>
<% end %>

<div id="create-garden-button">
  <% if !@user.garden %>
    <div>
      <%= form_for([@user, @garden]) do |f| %>
        <%= f.submit %>
      <% end %>
    </div>
  <% else %>
    <%= link_to "Delete Garden", user_garden_path(@user, @user.garden), method: :delete, data: {confirm: "Are you sure you want to delete your garden?"} %>
  <% end %>
  <%= link_to "Delete Profile", user_path(@user), method: :delete, data: {confirm: "Are you sure you want to delete your profile?"} %>
</div>